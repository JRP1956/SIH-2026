fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlalchemy==2.0.41
psycopg[binary]==3.2.10
pydantic-settings==2.7.0
email-validator==2.2.0
python-multipart==0.0.32
pyjwt==2.13.0
bcrypt==4.2.1
httpx==0.28.1
openai==1.59.6
lizard==1.17.10
semgrep==1.175.0
pytest==9.1.1
setuptools==84.0.0

# Security floors for transitive dependencies. Upstream specifiers here are open
# (requests wants idna>=2.5, semgrep wants tqdm>=4.9.0), so a resolver that picks
# the lowest satisfying version lands on releases with known advisories. The
# lockfile already resolves well above these; the floors make every resolver --
# including the scanners that read requirements.txt directly -- agree.
idna>=3.15
tqdm>=4.66.3
