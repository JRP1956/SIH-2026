services:
  backend:
    volumes:
      - ./backend:/app
      - /app/venv
      - /app/__pycache__
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]

  worker:
    volumes:
      - ./backend:/app
      - /app/venv
      - /app/__pycache__

